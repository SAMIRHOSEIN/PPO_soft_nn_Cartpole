# Python 3.10
# Core dependencies for training, evaluating, and plotting the
# PPO Soft Decision Tree vs. Neural Network experiments on CartPole-v1.
#
# NOTE: torchrl and tensordict are tightly version-coupled; pin the exact
# versions used for training to guarantee reproducibility.
torch
torchrl
tensordict
gymnasium
numpy
matplotlib
tqdm
